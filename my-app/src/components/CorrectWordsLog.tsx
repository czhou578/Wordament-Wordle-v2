
interface Props {
  correctWordsList: string[]
}

export default function CorrectWordsLog(props: Props) {
  // console.log(': ' + props.correctWordsList);
  return (
    <div>
      <div>

      </div>
    </div>
  )
}